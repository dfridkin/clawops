// src/mcp/tools/_generated.ts — GENERATED. Do not hand-edit.
// Generated by: pnpm gen:schemas (scripts/gen-schemas.ts)
// Source of truth: spec/mcp-tools.yaml

import { z } from 'zod'

export type Toolset = 'cli' | 'workflow' | 'read' | 'admin'

// ── clawops_status ──────────────────────────────────────────────────────────

export const clawops_statusSchema = z.object({
  stackName: z.string().optional(),
})

export const clawops_statusAnnotations = {
  title: "Get Stack Status",
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli","read"] as const,
} as const

export type StatusInput = z.infer<typeof clawops_statusSchema>

// ── clawops_logs_tail ───────────────────────────────────────────────────────

export const clawops_logs_tailSchema = z.object({
  stackName: z.string().optional(),
  tailLines: z.number().int().default(100),
  sinceMin: z.number().int().optional(),
})

export const clawops_logs_tailAnnotations = {
  title: "Tail Gateway Logs",
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli","read"] as const,
} as const

export type LogsTailInput = z.infer<typeof clawops_logs_tailSchema>

// ── clawops_stacks_list ─────────────────────────────────────────────────────

export const clawops_stacks_listSchema = z.object({})

export const clawops_stacks_listAnnotations = {
  title: "List Stacks",
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
  toolsets: ["admin","read"] as const,
} as const

export type StacksListInput = z.infer<typeof clawops_stacks_listSchema>

// ── clawops_config_get ──────────────────────────────────────────────────────

export const clawops_config_getSchema = z.object({
  stackName: z.string().optional(),
  key: z.string(),
})

export const clawops_config_getAnnotations = {
  title: "Get OpenClaw Config Value",
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli","read"] as const,
} as const

export type ConfigGetInput = z.infer<typeof clawops_config_getSchema>

// ── clawops_agents_list ─────────────────────────────────────────────────────

export const clawops_agents_listSchema = z.object({
  stackName: z.string().optional(),
})

export const clawops_agents_listAnnotations = {
  title: "List OpenClaw Agents",
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli","read"] as const,
} as const

export type AgentsListInput = z.infer<typeof clawops_agents_listSchema>

// ── clawops_up ──────────────────────────────────────────────────────────────

export const clawops_upSchema = z.object({
  stackName: z.string().optional(),
  provider: z.enum(['aws', 'gcp', 'azure', 'local']).optional(),
  region: z.string().optional(),
  instanceType: z.enum(['micro', 'small', 'medium', 'large', 'gpu']).default("small"),
  openclawVersion: z.string().optional(),
  dryRun: z.boolean().default(false),
})

export const clawops_upAnnotations = {
  title: "Provision and Deploy Stack",
  readOnlyHint: false,
  destructiveHint: true,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli"] as const,
} as const

export type UpInput = z.infer<typeof clawops_upSchema>

// ── clawops_destroy ─────────────────────────────────────────────────────────

export const clawops_destroySchema = z.object({
  stackName: z.string(),
  yes: z.boolean().default(false),
})

export const clawops_destroyAnnotations = {
  title: "Destroy Stack (DESTRUCTIVE)",
  readOnlyHint: false,
  destructiveHint: true,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli"] as const,
} as const

export type DestroyInput = z.infer<typeof clawops_destroySchema>

// ── clawops_apply ───────────────────────────────────────────────────────────

export const clawops_applySchema = z.object({
  planPath: z.string(),
  yes: z.boolean().default(false),
})

export const clawops_applyAnnotations = {
  title: "Apply Maker Plan",
  readOnlyHint: false,
  destructiveHint: true,
  idempotentHint: false,
  openWorldHint: true,
  toolsets: ["cli"] as const,
} as const

export type ApplyInput = z.infer<typeof clawops_applySchema>

// ── clawops_plan ────────────────────────────────────────────────────────────

export const clawops_planSchema = z.object({
  stackName: z.string().optional(),
  provider: z.enum(['aws', 'gcp', 'azure', 'local']).optional(),
  region: z.string().optional(),
  instanceType: z.enum(['micro', 'small', 'medium', 'large', 'gpu']).optional(),
  outPath: z.string().optional(),
})

export const clawops_planAnnotations = {
  title: "Generate Deploy Plan",
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli","read"] as const,
} as const

export type PlanInput = z.infer<typeof clawops_planSchema>

// ── clawops_config_set ──────────────────────────────────────────────────────

export const clawops_config_setSchema = z.object({
  stackName: z.string().optional(),
  key: z.string(),
  value: z.string(),
  restart: z.boolean().default(false),
})

export const clawops_config_setAnnotations = {
  title: "Set OpenClaw Config Value",
  readOnlyHint: false,
  destructiveHint: true,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli"] as const,
} as const

export type ConfigSetInput = z.infer<typeof clawops_config_setSchema>

// ── clawops_agents_restart ──────────────────────────────────────────────────

export const clawops_agents_restartSchema = z.object({
  stackName: z.string().optional(),
  agentId: z.string(),
})

export const clawops_agents_restartAnnotations = {
  title: "Restart OpenClaw Agent",
  readOnlyHint: false,
  destructiveHint: true,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli"] as const,
} as const

export type AgentsRestartInput = z.infer<typeof clawops_agents_restartSchema>

// ── clawops_gateway_restart ─────────────────────────────────────────────────

export const clawops_gateway_restartSchema = z.object({
  stackName: z.string().optional(),
})

export const clawops_gateway_restartAnnotations = {
  title: "Restart Gateway Daemon",
  readOnlyHint: false,
  destructiveHint: true,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["cli"] as const,
} as const

export type GatewayRestartInput = z.infer<typeof clawops_gateway_restartSchema>

// ── clawops_workflow_deploy_app ─────────────────────────────────────────────

export const clawops_workflow_deploy_appSchema = z.object({
  provider: z.enum(['aws', 'gcp', 'azure', 'local']),
  region: z.string().optional(),
  stackName: z.string().default("default"),
  instanceType: z.enum(['micro', 'small', 'medium', 'large', 'gpu']).default("small"),
})

export const clawops_workflow_deploy_appAnnotations = {
  title: "Deploy OpenClaw (End-to-End Workflow)",
  readOnlyHint: false,
  destructiveHint: true,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["workflow"] as const,
} as const

export type WorkflowDeployAppInput = z.infer<typeof clawops_workflow_deploy_appSchema>

// ── clawops_workflow_recover ────────────────────────────────────────────────

export const clawops_workflow_recoverSchema = z.object({
  stackName: z.string().optional(),
})

export const clawops_workflow_recoverAnnotations = {
  title: "Recover/Diagnose Stack",
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: true,
  toolsets: ["workflow","read"] as const,
} as const

export type WorkflowRecoverInput = z.infer<typeof clawops_workflow_recoverSchema>

// ── clawops_task_status ─────────────────────────────────────────────────────

export const clawops_task_statusSchema = z.object({
  taskId: z.string(),
})

export const clawops_task_statusAnnotations = {
  title: "Get Task Status",
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
  toolsets: ["cli","read"] as const,
} as const

export type TaskStatusInput = z.infer<typeof clawops_task_statusSchema>

// ── TOOLSETS registry ─────────────────────────────────────────────────────────

export const TOOLSETS: Record<Toolset, string[]> = {
  cli: [
    'clawops_status',
    'clawops_logs_tail',
    'clawops_config_get',
    'clawops_agents_list',
    'clawops_up',
    'clawops_destroy',
    'clawops_apply',
    'clawops_plan',
    'clawops_config_set',
    'clawops_agents_restart',
    'clawops_gateway_restart',
    'clawops_task_status',
  ],
  workflow: [
    'clawops_workflow_deploy_app',
    'clawops_workflow_recover',
  ],
  read: [
    'clawops_status',
    'clawops_logs_tail',
    'clawops_stacks_list',
    'clawops_config_get',
    'clawops_agents_list',
    'clawops_plan',
    'clawops_workflow_recover',
    'clawops_task_status',
  ],
  admin: [
    'clawops_stacks_list',
  ],
}
