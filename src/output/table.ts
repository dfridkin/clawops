// ASCII table renderer — not yet implemented (M1).

export function renderTable(
  _headers: string[],
  _rows: string[][],
): string {
  throw new Error('table renderer: not yet implemented (M1)')
}
