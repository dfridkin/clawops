// clawops — programmatic library entry
// Exposes stable public types for embedding clawops in other tools.

export type {
  ProviderAdapter,
  ProviderName,
  InstanceAlias,
  CredentialSource,
  StateBackendScheme,
  ConnectionInfo,
  BaseStackOutputs,
  StackOutputs,
  ValidationResult,
  PulumiFn,
} from './providers/types'
