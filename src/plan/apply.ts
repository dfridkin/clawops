// Maker plan apply — not yet implemented (M6).

import type { DeployPlan } from './generate'

export async function applyPlan(_plan: DeployPlan): Promise<void> {
  throw new Error('plan apply: not yet implemented (M6)')
}
