/**
 * OpenClaw compatibility, stated on the landing page rather than buried in docs.
 *
 * "Does this work with 2.0?" is the first question a visitor has. Answering it above the fold
 * costs one line and saves a support round-trip. This line requires 2.0; the previous one is
 * maintained under the `legacy` dist-tag.
 *
 * Shared, because the landing page and the social card both state it and a version that has to
 * be edited in two places is a version that goes stale in one of them.
 */
export const OPENCLAW_SUPPORTED = '2026.9.2'
