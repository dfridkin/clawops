---
'@clawops/cli': patch
---

`clawops init` generates an SSH key clawops can read.

If you ran `clawops init` before this release, `clawops doctor` reports whether your key is
usable and what to do if it is not.
