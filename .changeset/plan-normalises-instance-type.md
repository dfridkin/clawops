---
'@clawops/cli': patch
---

`clawops plan --instance-type` takes a clawops alias (`micro`–`gpu`) or a machine type your
cloud names itself, and the plan records the concrete type the cloud will be asked for.
